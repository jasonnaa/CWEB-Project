
@{ ViewBag.Title = "ConditionsLoops";}
<h4>Simple if statement</h4>

@{ 
    var show_Today = true;
    if(show_Today)
    {
        @DateTime.Today;
    }
}

<h4>if - else statement</h4>
@{ 
    var showToday = false;
    if(showToday)
    {
        @DateTime.Today;
    }
    else
    {
        <text> Sorry! </text>
    }
}

<h4>if - else if - else statement</h4>
@{ 
    var theBalance = 4.99;
    if(theBalance == 0)
    {
        <p>You have a zero balance.</p>
    }
    else if (theBalance > 0 && theBalance <= 5)
    { 
        <p>Your balance of $@theBalance is very low.</p>
    }
    else
    {
        <p>Your balance is: $@theBalance</p>
    }
}

<h4>switch-case statement</h4>
@{ 
    var weekday = "Wednesday";
    var greeting = "";

    switch(weekday)
    {
        case "Monday":
            greeting = "Ok, it's a marvelous Monday!";
            break;
        case "Tuesday":
            greeting = "It's a tremendous Tuesday!";
            break;
        case "Wednesday":
            greeting = "Wild Wednesday is here!";
            break;
        default:
            greeting = "It's some other day, oh well.";
            break;
    }

    <p>Since it is @weekday, the message for today is: @greeting</p>
}

<h4>For Loop</h4>
@for (var i = 10; i < 15; i++)
 {
    <p>Line #: @i</p>
 }


@{
    string[] devices = { "Keyboard", "Mouse", "Printer", "scanner" };
    int[] mynums = { 100, 240, 189, 40 };
}

<p>Output Device: @devices[2]</p>
<p>Multiples of Three: @mynums[1], @mynums[2]</p>

<h4>Foreach Loop</h4>
<ul>
    @foreach (var myItem in Request.ServerVariables)
     {
        <li>@myItem</li>
     }



</ul>

@{
    string[] colors = { "green", "blue", "orange", "black" };
    foreach (var myItem in colors.Select((value, i) => new { i, value}))
    {
        <p>The index is @myItem.i and a value is @myItem.value </p>
    }
}

<h4>While Loop</h4>
@{ 
    var countNum = 0;
    while (countNum < 10)
    {
        countNum += 1;
        <p>Line #@countNum: </p>
    }
}

